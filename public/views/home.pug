extends layout
block content
    .searchForm.flex-center-column
        form(action="/search/nearby" method="POST")
            .searchFields
                input(type="text" name="location" placeholder="Where are you?")
                button.btn.btn-primary Submit